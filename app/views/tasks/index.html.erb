<ul>
  <% @tasks.each do |task| %>
    <li><%=link_to task.title, task_path(task)%></li>
    <li><%= task.details %></li>
    <li><%= task.completed %></li>
  <% end %>
</ul>

<%#= link_to "add a task", task_path %>
